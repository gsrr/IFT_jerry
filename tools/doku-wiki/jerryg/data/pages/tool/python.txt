====== object analysis======
  * vars(object) , but not all objects have a __dict__ member
  * dir(object)
  * getattr()
  * type()
  * 
<file>
def dump(obj):
  for attr in dir(obj):
    print "obj.%s = %s" % (attr, getattr(obj, attr))
</file> 