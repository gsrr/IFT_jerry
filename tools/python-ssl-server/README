[passphrase]
A passphrase is a sequence of words or other text used to control access to a computer system, program or data. A passphrase is similar to a password in usage.

[Remove the key passphrase]
openssl rsa -in key.pem -out newkey.pem

[Remove the key passphrase(certificate and key are together)]
openssl rsa -in mycert.pem -out newcert.pem
openssl x509 -in mycert.pem >>newcert.pem

